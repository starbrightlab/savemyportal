export default function ProBadge() {
    return (
        <span className="inline-flex items-center gap-1 px-2.5 py-1 text-sm font-bold uppercase tracking-wider text-soft-gold bg-soft-gold/10 border border-soft-gold/20 rounded">
            Pro
        </span>
    );
}
